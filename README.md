# gallery-gen

Usage:

    $ gem install gallery-gen
    $ rm -rf _site _data
    $ mkdir galleries
    $ cp -va <pics> galleries
    $ vi _config.yml

    $ gallery-gen

	$ jekyll build
    $ jekyll serve
